<Window x:Class="WeatherWidget.App.UI.EmbedExperimentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="任务栏嵌入方案实验"
        Width="700" Height="550"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 标题和说明 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="任务栏嵌入方案实验" FontSize="18" FontWeight="Bold" Foreground="White" />
            <TextBlock Text="同时创建多个测试窗口，验证不同嵌入策略的可行性" Foreground="#888" Margin="0,4,0,0" />
        </StackPanel>

        <!-- 日志输出 -->
        <Border Grid.Row="1" Background="#0D0D0D" CornerRadius="8" Padding="8">
            <ScrollViewer x:Name="LogScrollViewer" VerticalScrollBarVisibility="Auto">
                <TextBox x:Name="LogTextBox"
                         IsReadOnly="True"
                         Background="Transparent"
                         Foreground="#00FF00"
                         FontFamily="Consolas"
                         FontSize="12"
                         BorderThickness="0"
                         TextWrapping="Wrap"
                         AcceptsReturn="True" />
            </ScrollViewer>
        </Border>

        <!-- 操作按钮 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="RunButton" Content="运行实验" Width="100" Height="32" Margin="0,0,8,0" Click="RunButton_Click" />
            <Button x:Name="StatusButton" Content="刷新状态" Width="100" Height="32" Margin="0,0,8,0" Click="StatusButton_Click" IsEnabled="False" />
            <Button x:Name="BringTopButton" Content="提升窗口" Width="100" Height="32" Margin="0,0,8,0" Click="BringTopButton_Click" IsEnabled="False" />
            <Button x:Name="CleanupButton" Content="清理窗口" Width="100" Height="32" Click="CleanupButton_Click" IsEnabled="False" />
        </StackPanel>
    </Grid>
</Window>
